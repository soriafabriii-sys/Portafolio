import {COLORS} from '../../utils/constants';import {t} from '../../utils/translations';import {useLanguage} from '../../hooks/useLanguage';
export default function GalleryPagination({totalGroups,currentGroup,onGroupChange}){const {language}=useLanguage();return (<div style={{display:'flex',justifyContent:'center',gap:'12px',flexWrap:'wrap',marginTop:'60px'}}>{Array.from({length:totalGroups},(_,i)=>i+1).map(group=>(<button key={group} onClick={()=>onGroupChange(group)} style={{minWidth:'120px',padding:'10px 16px',border:currentGroup===group?'none':`1px solid ${COLORS.bgAlt}`,borderRadius:'8px',backgroundColor:currentGroup===group?COLORS.primary:'transparent',color:currentGroup===group?COLORS.bg:COLORS.textPrimary,cursor:'pointer',fontSize:'14px',fontWeight:'600',transition:'all 300ms ease'}} onMouseEnter={e=>{if(currentGroup!==group){e.target.style.borderColor=COLORS.primary;e.target.style.color=COLORS.primary;}}} onMouseLeave={e=>{if(currentGroup!==group){e.target.style.borderColor=COLORS.bgAlt;e.target.style.color=COLORS.textPrimary;}}}>{t('viewGroup',language)} {group}</button>))}</div>);
}
