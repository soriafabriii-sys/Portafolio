import {useAppContext} from '../../hooks/useAppContext';import {useLightbox} from '../../hooks/useLightbox';import {useLanguage} from '../../hooks/useLanguage';import {designsByCategory} from '../../data/designs';import {t} from '../../utils/translations';import {COLORS} from '../../utils/constants';import Icon from '../Common/Icon';import {useMemo} from 'react';
export default function Lightbox(){const {isLightboxOpen,selectedImage:contextImage}=useAppContext();const {language}=useLanguage();const allDesigns=useMemo(()=>{const all=[];for(const category in designsByCategory){all.push(...designsByCategory[category]);}return all;},[]);const {selectedImage,isOpen,close,goToPrevious,goToNext,hasPrevious,hasNext,currentIndex,total}=useLightbox(contextImage?.lightboxItems || allDesigns);if(!isOpen||!selectedImage)return null;
return (<div style={{position:'fixed',inset:0,backgroundColor:COLORS.overlay,display:'flex',alignItems:'center',justifyContent:'center',zIndex:1000,backdropFilter:'blur(4px)'}} onClick={close}><div style={{position:'relative',maxWidth:'90vw',maxHeight:'90vh',display:'flex',flexDirection:'column',backgroundColor:COLORS.bgAlt,borderRadius:'12px',overflow:'hidden',border:`1px solid ${COLORS.bgAlt}`}} onClick={e=>e.stopPropagation()}><div style={{flex:1,display:'flex',alignItems:'center',justifyContent:'center',position:'relative',minHeight:'400px'}}>{/(mp4|webm|mkv|avi)$/i.test(selectedImage.src || '') ? (
  <video src={selectedImage.src} controls autoPlay muted loop playsInline style={{maxWidth:'100%',maxHeight:'100%',objectFit:'contain',backgroundColor:'#000'}}/>
) : (
  <img src={selectedImage.src} alt={selectedImage.alt} style={{maxWidth:'100%',maxHeight:'100%',objectFit:'contain'}}/>
)}{hasPrevious&&(<button onClick={goToPrevious} style={{position:'absolute',left:'20px',top:'50%',transform:'translateY(-50%)',backgroundColor:`${COLORS.primary}80`,border:'none',width:'48px',height:'48px',borderRadius:'50%',cursor:'pointer',display:'flex',alignItems:'center',justifyContent:'center',color:COLORS.textPrimary,transition:'all 300ms ease'}} onMouseEnter={e=>{e.currentTarget.style.backgroundColor=COLORS.primary;e.currentTarget.style.transform='translateY(-50%) scale(1.1)';}} onMouseLeave={e=>{e.currentTarget.style.backgroundColor=`${COLORS.primary}80`;e.currentTarget.style.transform='translateY(-50%) scale(1)';}}><Icon name="chevron-left" size={24}/></button>)}{hasNext&&(<button onClick={goToNext} style={{position:'absolute',right:'20px',top:'50%',transform:'translateY(-50%)',backgroundColor:`${COLORS.primary}80`,border:'none',width:'48px',height:'48px',borderRadius:'50%',cursor:'pointer',display:'flex',alignItems:'center',justifyContent:'center',color:COLORS.textPrimary,transition:'all 300ms ease'}} onMouseEnter={e=>{e.currentTarget.style.backgroundColor=COLORS.primary;e.currentTarget.style.transform='translateY(-50%) scale(1.1)';}} onMouseLeave={e=>{e.currentTarget.style.backgroundColor=`${COLORS.primary}80`;e.currentTarget.style.transform='translateY(-50%) scale(1)';}}><Icon name="chevron-right" size={24}/></button>)}</div><div style={{backgroundColor:COLORS.bg,borderTop:`1px solid ${COLORS.bgAlt}`,padding:'20px',display:'flex',justifyContent:'space-between',alignItems:'center'}}><div style={{flex:1}}><h3 style={{margin:'0 0 8px 0',color:COLORS.textPrimary,fontSize:'16px',fontWeight:'600'}}>{selectedImage.alt}</h3><p style={{margin:0,color:COLORS.textMuted,fontSize:'12px'}}>{currentIndex} {t('of',language)} {total}</p></div><button onClick={close} style={{backgroundColor:'transparent',border:'none',cursor:'pointer',color:COLORS.textPrimary,display:'flex',alignItems:'center',justifyContent:'center',width:'40px',height:'40px',borderRadius:'6px',transition:'all 300ms ease'}} onMouseEnter={e=>{e.currentTarget.style.backgroundColor=COLORS.bgAlt;}} onMouseLeave={e=>{e.currentTarget.style.backgroundColor='transparent';}}><Icon name="x" size={24}/></button></div></div></div>);
}
